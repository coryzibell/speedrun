name: Release

on:
  push:
    branches:
      - release

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: speedrun
            asset_name: speedrun-linux-x86_64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            artifact_name: speedrun
            asset_name: speedrun-linux-aarch64
          - os: ubuntu-latest
            target: x86_64-unknown-freebsd
            artifact_name: speedrun
            asset_name: speedrun-freebsd-x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: speedrun.exe
            asset_name: speedrun-windows-x86_64.exe
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            artifact_name: speedrun.exe
            asset_name: speedrun-windows-aarch64.exe
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: speedrun
            asset_name: speedrun-macos-x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact_name: speedrun
            asset_name: speedrun-macos-aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross (cross-compilation targets)
        if: matrix.target == 'aarch64-unknown-linux-gnu' || matrix.target == 'x86_64-unknown-freebsd'
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build x86_64-unknown-linux-gnu
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build aarch64-unknown-linux-gnu
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: cross build --release --target ${{ matrix.target }}

      - name: Build x86_64-unknown-freebsd
        if: matrix.target == 'x86_64-unknown-freebsd'
        run: cross build --release --target ${{ matrix.target }}

      - name: Build x86_64-pc-windows-msvc
        if: matrix.target == 'x86_64-pc-windows-msvc'
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build aarch64-pc-windows-msvc
        if: matrix.target == 'aarch64-pc-windows-msvc'
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build x86_64-apple-darwin
        if: matrix.target == 'x86_64-apple-darwin'
        run: cargo build --release --target ${{ matrix.target }}

      - name: Build aarch64-apple-darwin
        if: matrix.target == 'aarch64-apple-darwin'
        run: cargo build --release --target ${{ matrix.target }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}
          path: target/${{ matrix.target }}/release/${{ matrix.artifact_name }}

      - name: Package binary (Linux/FreeBSD)
        if: contains(matrix.target, 'linux') || contains(matrix.target, 'freebsd')
        run: |
          cd target/${{ matrix.target }}/release
          tar -czf ${{ matrix.asset_name }}.tar.gz ${{ matrix.artifact_name }}
          mv ${{ matrix.asset_name }}.tar.gz ../../..

      - name: Package binary (Windows)
        if: contains(matrix.target, 'windows')
        shell: pwsh
        run: |
          Compress-Archive -Path target/${{ matrix.target }}/release/${{ matrix.artifact_name }} -DestinationPath ${{ matrix.asset_name }}.zip

      - name: Package binary (macOS)
        if: contains(matrix.target, 'apple')
        run: |
          cd target/${{ matrix.target }}/release
          tar -czf ${{ matrix.asset_name }}.tar.gz ${{ matrix.artifact_name }}
          mv ${{ matrix.asset_name }}.tar.gz ../../..

      - name: Upload packaged artifact (Linux/FreeBSD)
        if: contains(matrix.target, 'linux') || contains(matrix.target, 'freebsd')
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}-packaged
          path: ${{ matrix.asset_name }}.tar.gz

      - name: Upload packaged artifact (Windows)
        if: contains(matrix.target, 'windows')
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}-packaged
          path: ${{ matrix.asset_name }}.zip

      - name: Upload packaged artifact (macOS)
        if: contains(matrix.target, 'apple')
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.asset_name }}-packaged
          path: ${{ matrix.asset_name }}.tar.gz

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Get version from Cargo.toml
          VERSION="v$(grep '^version' Cargo.toml | head -1 | cut -d'"' -f2)"
          
          # Check if release already exists
          if gh release view "$VERSION" >/dev/null 2>&1; then
            echo "Release $VERSION already exists, skipping"
            exit 0
          fi
          
          # Delete tag if it exists without a release
          git push origin :refs/tags/"$VERSION" 2>/dev/null || true
          
          # Create new tag
          git tag "$VERSION"
          git push origin "$VERSION"
          
          # Create release
          gh release create "$VERSION" \
            --title "Release $VERSION" \
            --generate-notes
          
          # Upload all packaged binaries
          for dir in artifacts/*-packaged/; do
            if [ -d "$dir" ]; then
              for file in "$dir"*.tar.gz "$dir"*.zip; do
                if [ -f "$file" ]; then
                  gh release upload "$VERSION" "$file"
                fi
              done
            fi
          done
